---
export interface Props {
  title: string
  description: string
  locale: string
  adsenseClient: string
  noindex?: boolean
  canonicalUrl?: string
}

const { 
  title, 
  description, 
  locale, 
  adsenseClient, 
  noindex = false,
  canonicalUrl 
} = Astro.props
---

<!DOCTYPE html>
<html lang={locale} class="scroll-smooth">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  
  <!-- Primary Meta Tags -->
  <title>{title}</title>
  <meta name="title" content={title}>
  <meta name="description" content={description}>
  
  <!-- Favicon -->
  <link rel="icon" type="image/svg+xml" href="/favicon.svg">
  <link rel="icon" type="image/png" href="/favicon.png">
  
  <!-- Open Graph / Facebook -->
  <meta property="og:type" content="website">
  <meta property="og:url" content={Astro.url}>
  <meta property="og:title" content={title}>
  <meta property="og:description" content={description}>
  <meta property="og:image" content={`${Astro.site}/og-image.jpg`}>
  
  <!-- Twitter -->
  <meta property="twitter:card" content="summary_large_image">
  <meta property="twitter:url" content={Astro.url}>
  <meta property="twitter:title" content={title}>
  <meta property="twitter:description" content={description}>
  <meta property="twitter:image" content={`${Astro.site}/og-image.jpg`}>
  
  <!-- SEO -->
  {canonicalUrl && <link rel="canonical" href={canonicalUrl}>}
  {noindex && <meta name="robots" content="noindex, nofollow">}
  
  <!-- AdSense -->
  {adsenseClient && (
    <script async src="https://pagead2.googlesyndication.com/pagead/js/adsbygoogle.js?client={adsenseClient}"
            crossorigin="anonymous"></script>
  )}
  
  <!-- Performance -->
  <link rel="preload" href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800&display=swap" as="style">
  <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800&display=swap">
  
  <!-- Mobile Optimization -->
  <meta name="theme-color" content="#3b82f6">
  <meta name="apple-mobile-web-app-capable" content="yes">
  <meta name="apple-mobile-web-app-status-bar-style" content="default">
  
  <!-- Structured Data -->
  <script type="application/ld+json">
    {
      "@context": "https://schema.org",
      "@type": "WebSite",
      "name": title,
      "description": description,
      "url": Astro.site
    }
  </script>
  
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }
    
    html {
      font-family: 'Inter', system-ui, -apple-system, sans-serif;
      scroll-behavior: smooth;
    }
    
    body {
      background: #fafafa;
      color: #1f2937;
      line-height: 1.6;
      font-size: 16px;
    }
    
    .container {
      max-width: 1200px;
      margin: 0 auto;
      padding: 0 1rem;
    }
    
    @media (min-width: 768px) {
      .container {
        padding: 0 2rem;
      }
    }
    
    /* Modern Mobile Navigation */
    .mobile-nav {
      display: none;
      position: fixed;
      top: 0;
      left: 0;
      right: 0;
      bottom: 0;
      background: rgba(0, 0, 0, 0.95);
      z-index: 1000;
      padding: 2rem;
    }
    
    .mobile-nav.active {
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
    }
    
    /* Responsive Typography */
    h1 { font-size: clamp(1.75rem, 4vw, 2.5rem); font-weight: 800; }
    h2 { font-size: clamp(1.5rem, 3vw, 2rem); font-weight: 700; }
    h3 { font-size: clamp(1.25rem, 2.5vw, 1.5rem); font-weight: 600; }
    h4 { font-size: clamp(1.125rem, 2vw, 1.25rem); font-weight: 600; }
    
    /* Loading Animation */
    .loading {
      opacity: 0;
      animation: fadeIn 0.5s ease-in-out forwards;
    }
    
    @keyframes fadeIn {
      to { opacity: 1; }
    }
    
    /* Mobile-First Utilities */
    .hide-mobile { display: none; }
    .show-mobile { display: block; }
    
    @media (min-width: 768px) {
      .hide-mobile { display: block; }
      .show-mobile { display: none; }
    }
  </style>
</head>

<body class="loading">
  <!-- Header -->
  <header class="bg-white shadow-sm sticky top-0 z-50">
    <div class="container">
      <nav class="flex items-center justify-between py-4">
        <!-- Logo -->
        <div class="flex items-center space-x-3">
          <div class="w-10 h-10 bg-gradient-to-r from-blue-500 to-purple-600 rounded-lg flex items-center justify-center">
            <span class="text-white font-bold text-xl">📝</span>
          </div>
          <span class="font-bold text-xl text-gray-900">{Astro.site?.hostname || 'Blog'}</span>
        </div>
        
        <!-- Desktop Navigation -->
        <div class="hidden md:flex items-center space-x-8">
          <a href="/" class="text-gray-600 hover:text-blue-600 transition-colors">Ana Sayfa</a>
          <a href="/articles" class="text-gray-600 hover:text-blue-600 transition-colors">Makaleler</a>
          <a href="/about" class="text-gray-600 hover:text-blue-600 transition-colors">Hakkımızda</a>
          <a href="/contact" class="text-gray-600 hover:text-blue-600 transition-colors">İletişim</a>
        </div>
        
        <!-- Mobile Menu Button -->
        <button class="md:hidden p-2 text-gray-600" onclick="toggleMobileMenu()">
          <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"></path>
          </svg>
        </button>
      </nav>
    </div>
  </header>
  
  <!-- Mobile Navigation -->
  <div class="mobile-nav" id="mobileNav">
    <button class="absolute top-4 right-4 text-white text-2xl" onclick="toggleMobileMenu()">×</button>
    <div class="space-y-8 text-center">
      <a href="/" class="block text-white text-xl font-medium">Ana Sayfa</a>
      <a href="/articles" class="block text-white text-xl font-medium">Makaleler</a>
      <a href="/about" class="block text-white text-xl font-medium">Hakkımızda</a>
      <a href="/contact" class="block text-white text-xl font-medium">İletişim</a>
    </div>
  </div>
  
  <!-- Main Content -->
  <main class="min-h-screen">
    <slot />
  </main>
  
  <!-- Footer -->
  <footer class="bg-gray-900 text-white py-12 mt-16">
    <div class="container">
      <div class="grid grid-cols-1 md:grid-cols-4 gap-8">
        <!-- Site Info -->
        <div class="md:col-span-2">
          <h3 class="font-bold text-lg mb-4">{Astro.site?.hostname || 'Blog'}</h3>
          <p class="text-gray-300 mb-4">{description}</p>
          <div class="flex space-x-4">
            <a href="#" class="text-gray-300 hover:text-white">📘</a>
            <a href="#" class="text-gray-300 hover:text-white">🐦</a>
            <a href="#" class="text-gray-300 hover:text-white">📸</a>
            <a href="#" class="text-gray-300 hover:text-white">📧</a>
          </div>
        </div>
        
        <!-- Quick Links -->
        <div>
          <h4 class="font-semibold mb-4">Hızlı Linkler</h4>
          <ul class="space-y-2 text-gray-300">
            <li><a href="/" class="hover:text-white">Ana Sayfa</a></li>
            <li><a href="/articles" class="hover:text-white">Makaleler</a></li>
            <li><a href="/about" class="hover:text-white">Hakkımızda</a></li>
            <li><a href="/contact" class="hover:text-white">İletişim</a></li>
          </ul>
        </div>
        
        <!-- Legal -->
        <div>
          <h4 class="font-semibold mb-4">Yasal</h4>
          <ul class="space-y-2 text-gray-300">
            <li><a href="/privacy" class="hover:text-white">Gizlilik Politikası</a></li>
            <li><a href="/terms" class="hover:text-white">Kullanım Şartları</a></li>
            <li><a href="/cookies" class="hover:text-white">Çerez Politikası</a></li>
            <li><a href="/sitemap.xml" class="hover:text-white">Site Haritası</a></li>
          </ul>
        </div>
      </div>
      
      <div class="border-t border-gray-800 mt-8 pt-8 text-center text-gray-400">
        <p>&copy; {new Date().getFullYear()} {Astro.site?.hostname || 'Blog'}. Tüm hakları saklıdır.</p>
      </div>
    </div>
  </footer>
  
  <!-- JavaScript -->
  <script>
    // Mobile Menu Toggle
    function toggleMobileMenu() {
      const mobileNav = document.getElementById('mobileNav');
      mobileNav.classList.toggle('active');
    }
    
    // Close mobile menu when clicking outside
    document.addEventListener('click', (e) => {
      const mobileNav = document.getElementById('mobileNav');
      const menuButton = document.querySelector('[onclick="toggleMobileMenu()"]');
      
      if (!mobileNav.contains(e.target) && !menuButton.contains(e.target)) {
        mobileNav.classList.remove('active');
      }
    });
    
    // Smooth scroll for anchor links
    document.querySelectorAll('a[href^="#"]').forEach(anchor => {
      anchor.addEventListener('click', function (e) {
        e.preventDefault();
        const target = document.querySelector(this.getAttribute('href'));
        if (target) {
          target.scrollIntoView({ behavior: 'smooth' });
        }
      });
    });
    
    // Loading animation
    document.addEventListener('DOMContentLoaded', () => {
      document.body.classList.add('loading');
    });
    
    // Performance: Lazy load images
    if ('IntersectionObserver' in window) {
      const imageObserver = new IntersectionObserver((entries, observer) => {
        entries.forEach(entry => {
          if (entry.isIntersecting) {
            const img = entry.target;
            img.src = img.dataset.src;
            img.classList.remove('lazy');
            imageObserver.unobserve(img);
          }
        });
      });
      
      document.querySelectorAll('img[data-src]').forEach(img => {
        imageObserver.observe(img);
      });
    }
  </script>
</body>
</html>